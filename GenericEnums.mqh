//ENUM #################################################################################################################################################

enum BOOLEAN_TYPE {
    yes,                    // SIM
    no                      // NÃO
};

enum OPERATIONS_TYPE {
    buy,                    // Compra
    sell,                   // Venda
    all                     // Ambos Sentidos
};
