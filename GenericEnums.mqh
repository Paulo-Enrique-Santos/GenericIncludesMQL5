//ENUM #################################################################################################################################################

enum ENUM_BOOLEAN_TYPE {
    yes,                    // SIM
    no                      // NÃO
};

enum ENUM_OPERATIONS_TYPE {
    buy,                    // Compra
    sell,                   // Venda
    all                     // Ambos Sentidos
};
